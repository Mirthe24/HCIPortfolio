import processing.sound.*;

SoundFile file;
float L;
void setup(){
size(800,800);
background(255);
L=0;
file = new SoundFile(this,"Promise.mp3");
file.play();
file.amp(0.2);
//L=file.duration();
//file.rate(L);
//file.jump(30);
}

void draw(){
 // println(L);
 // L=L+0.1 ; 
  //file.rate(L);
}